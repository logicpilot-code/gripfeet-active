<section class="move-band">
  {%- if section.settings.image != blank -%}
    <img
      src="{{ section.settings.image | image_url: width: 2400 }}"
      alt="Move band"
      width="2400" height="900"
      loading="lazy"
      style="width:100%;height:100%;object-fit:cover;position:absolute;inset:0"
    >
  {%- else -%}
    <img src="{{ 'gf-move-band.jpg' | asset_url }}" alt="Feet in motion" width="2200" height="933" loading="lazy" style="width:100%;height:100%;object-fit:cover;object-position:center 55%;position:absolute;inset:0">
  {%- endif -%}
  <div class="scrim"></div>
  <h2>{{ section.settings.headline | newline_to_br }}</h2>
</section>

{% schema %}
{
  "name": "Gripfeet - Move Band",
  "tag": "section",
  "settings": [
    { "type": "textarea",     "id": "headline", "label": "Headline", "default": "Every Step, Every Day." },
    { "type": "image_picker", "id": "image",    "label": "Background image" }
  ],
  "presets": [{ "name": "Gripfeet - Move Band" }]
}
{% endschema %}